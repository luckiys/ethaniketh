# ─────────────────────────────────────────────────────────────────────────────
# AegisOS — Frontend environment template
# Copy to .env.local and fill in real values.
# All fields are optional — leave blank to run in fully-mocked demo mode.
# ─────────────────────────────────────────────────────────────────────────────

# ── Hedera ───────────────────────────────────────────────────────────────────
# Create a free testnet account: https://portal.hedera.com
# Account ID format:  0.0.12345
# Private key format: 302e020100300506032b657004220420...
HEDERA_OPERATOR_ID=0.0.xxxxx
HEDERA_OPERATOR_KEY=302e020100300506032b657004220420...

# HCS topic — auto-created on first run if blank
HEDERA_TOPIC_ID=

# HTS token for rebalance transfers (0.0.0 = simulated)
HEDERA_HTS_TOKEN_ID=0.0.0

# Destination account for demo HTS transfers
HEDERA_DEMO_TO_ACCOUNT=0.0.0

# iNFT NFT collection — auto-created on first run if blank
HEDERA_NFT_TOKEN_ID=

# AegisScheduler.sol address after Hardhat deploy
# cd contracts && npx hardhat run scripts/deploy.ts --network hederaTestnet
AEGIS_SCHEDULER_ADDRESS=

# ── 0g Labs ──────────────────────────────────────────────────────────────────
# EVM private key for 0g testnet — fund at https://faucet.0g.ai
# Blank = deterministic SHA-256 mock CIDs (works for demo)
ZEROG_PRIVATE_KEY=

# ── WalletConnect ─────────────────────────────────────────────────────────────
# Get a free project ID: https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=b56e18d47c72ab683b10814fe9495694

# ── Alchemy RPC (optional) ────────────────────────────────────────────────────
# Free key at alchemy.com — 300M compute units/month, more reliable than public RPCs
NEXT_PUBLIC_ALCHEMY_API_KEY=

# ── VaR Service (optional) ────────────────────────────────────────────────────
# Python GARCH/VaR service. Run: cd services/var && pip install -r requirements.txt && uvicorn var_api:app --port 5001
VAR_SERVICE_URL=http://localhost:5001

# ── Portfolio Optimization (optional) ─────────────────────────────────────────
# Python PyPortfolioOpt service. Run: cd services/portfolio && pip install -r requirements.txt && uvicorn portfolio_api:app --port 5002
PORTFOLIO_SERVICE_URL=http://localhost:5002

# ── FRED API (optional) ──────────────────────────────────────────────────────
# Free key at fred.stlouisfed.org — VIX, Fed Funds, macro regime
FRED_API_KEY=

# ── AI Reasoning (natural language strategy explanations) ─────────────────────
# Gemini is free with no credit card — get key at https://aistudio.google.com
GEMINI_API_KEY=
# Optional fallback: Anthropic Claude (requires billing)
ANTHROPIC_API_KEY=

# ── Hedera SDK-Only Bounty (separate topics/tokens from EVM path) ─────────────
# These are auto-created if blank (uses same operator key above)
# HCS receipt topic for SDK-only audit receipts (no Solidity)
HEDERA_RECEIPT_TOPIC_ID=
# HTS loyalty token for AegisPoints (auto-created from treasury = operator)
HEDERA_LOYALTY_TOKEN_ID=

# ── Hedera EVM path (for AegisScheduler.sol / Schedule Service bounty) ────────
# ECDSA private key for Hedera EVM JSON-RPC (different from ED25519 above)
# Fund at: https://portal.hedera.com → Convert ED25519 account to ECDSA
HEDERA_EVM_DEPLOYER_KEY=
# EVM address of deployed AegisScheduler.sol
# Deploy: cd contracts && npx hardhat run scripts/deploy.ts --network hederaTestnet
AEGIS_SCHEDULER_ADDRESS=
# EVM address of HTS token (if using HTS schedule path instead of HBAR)
HEDERA_HTS_TOKEN_EVM=
