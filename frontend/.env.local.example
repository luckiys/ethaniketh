# Optional: external API URL (default: use built-in /api routes)
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Get your free WalletConnect project ID from https://cloud.walletconnect.com
# Demo ID works on localhost only; use your own for production
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
