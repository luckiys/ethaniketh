{
  "name": "aegisos",
  "version": "1.0.0",
  "private": true,
  "description": "AegisOS — AI automation OS for DeFi safety + yield",
  "scripts": {
    "dev": "npm run build:shared && npm run dev --prefix frontend",
    "dev:all": "npm run build:shared && concurrently --names \"backend,frontend\" --prefix-colors \"cyan,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build:shared": "npm run build --prefix shared 2>/dev/null || (cd shared && npm install && npm run build)",
    "build": "npm run build:shared && npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "install:all": "npm install && npm install --prefix shared && npm run build --prefix shared && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
